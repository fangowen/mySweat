const closeIcon = document.getElementById('close-icon');

// Add click event listener to the close icon
closeIcon.addEventListener('click', () => {
  // Hide the icon when clicked
  closeIcon.style.display = 'none';
});